#!/bin/bash

CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
    go build -a -tags netgo \
      -ldflags '-w -extldflags "-static"' \
      -o rund .
gcloud builds submit -t gcr.io/hightowerlabs/rund:0.0.1 .

rm rund

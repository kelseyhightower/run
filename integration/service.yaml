apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: integration-tests
spec:
  traffic:
    - percent: 100
      latestRevision: true
  template:
    spec:
      containerConcurrency: 50
      containers:
        - image: gcr.io/hightowerlabs/run-integration:0.0.1
          resources:
            limits:
              memory: 1G
